import React from "react";
import "../styles/hero.styles.scss";

const Hero = () => {
  return (
    <section className="section__hero">
      <div className="hero">
        <div className="hero__text">
          {/* <h1>Products that are not just cheap!</h1> */}
        </div>
        <div className="hero__image"></div>
      </div>
    </section>
  );
};

export default Hero;
